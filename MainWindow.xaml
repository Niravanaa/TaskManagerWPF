<Window x:Class="TaskManagementApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Manager" Height="350" Width="580">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="5" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="100" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox Name="TaskListBox" Grid.Row="0" Margin="10" DisplayMemberPath="Title" SelectionMode="Single" ItemsSource="{Binding tasks}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBox Name="TaskInputBox" Width="200" Margin="0,0,10,0" Text="Enter a task..." />
            <Button Content="Add Task" Click="AddTask_Click" />
            <Button Content="Edit Task" Click="EditTask_Click" />
            <Button Content="Delete Task" Click="DeleteTask_Click" />
        </StackPanel>
    </Grid>
</Window>
